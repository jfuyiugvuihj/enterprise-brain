# 人工闸门清单（Human Gates，2026-09-17）

**基线**：分支 `codex/data-file-catalog` @ `6ab523d`（H9 结案与 H10 在其后的文档提交中加入）。**作者**：总控线。
**用途**：登记**只有用户本人能做、Agent 不得代做**的事项。每项都给出**机械可核对的触发条件**，
供后续 Agent / 心跳提醒在当前时点判断"现在该不该叫人"，而不是靠记性或摘要。

**给 Agent 的规矩**
1. 本清单里被标 **仅用户本人** 的动作，任何 Agent 一律不得执行（含下单、push、删文件、改忽略规则）。
2. 提醒前先跑该项的 **核对命令**；**核对命令不满足触发条件就不许提醒**（防空转）。
3. 提醒时必须说清：哪一项、为什么现在要动、最短路径、不做的代价。一次只报已到触发点的项。
4. 状态变化后**就地改本文件的「当前」行**并附 commit 号，不要另开一份。

---

## H1 租用算力做 E2/E3 定档实测 —— **仅用户本人**（09-17 订正：**先跑 H11 的本机零风险探针**，通过则本条暂缓）
- **为什么只能你**：注册、实名认证、充值、下单是账号级动作。
- **当前**：未做。成本已算死 **<200 元**（明细见 `docs/perf/enterprise-env-matrix.md` §5）。
- **触发条件**：腿②完成 **R25 + R26**（GPU 诚实声明）之后 —— 早于 R26 租卡等于白花钱，
  因为现在 compose 里没有设备声明，装了卡也用不上。
- **核对命令**（09-17 11:15 订正：原先写的 device_requests 字面量是错的，按它核对会把已落地误判成未落地）：
  git grep -n "reservations" -- docker-compose.yml 有输出＝R26 设备申请已落，可叫人。
  依据：官方 docs.docker.com/compose/how-tos/gpu-support/ 把 deploy.resources.reservations.devices
  （driver: nvidia + count + capabilities: [gpu]）列为 docker compose up（非 Swarm）的受支持写法；
  本机 Compose v5.5.1 实测 config 原样保留该块、count: all 规范化为 count: -1，不会改写成 device_requests。
  两个硬约束：capabilities 必填（否则部署报错）、count 与 device_ids 互斥。
- **不做的代价**：拿不到 GPU 档实测数字 ⇒ 计划书 §3.2 的"不可承诺"永远摘不掉，
  对外只能停在"任意问题 5–10 s 不成立"这一侧。

## H2 E2 虚机手装 Nvidia 驱动并**立刻**做成私有镜像 —— 仅用户本人（Agent 可在旁给命令）
- **当前**：未做。虚机是裸机纯净环境，含 Nouveau / kernel-headers 坑，重来一次 30–60 分钟。
- **触发条件**：H1 下单成功、实例开机即做，**不要先装别的**。
- **核对命令**：无（依赖 H1 完成）。
- **不做的代价**：每轮测试重装驱动；按量计费下每次多烧 1–2 小时钱。

## H3 Docker Desktop 需手动拉起（机器重启后） —— 仅用户本人
- **当前**：**在跑**（2026-09-17 实测 `docker ps` → 7 容器 Up：backend / worker / scheduler /
  frontend / redis / postgres / ollama）。本项此刻**不触发**。
- **触发条件**：机器重启或休眠后，容器端到端门禁 / 阶段 6–7 浏览器验收 / 任何真机数字被需要时。
- **核对命令**：`docker ps --format "{{.Names}}"`（连不上管道或空 ⇒ 叫人）。
- **不做的代价**：这是 **09-16 跨夜停摆的真实根因**（看板 §4G）。闸门刚开、环境没跟上，
  整晚白等——代码没卡住，是 Docker 没运行。

## H4 仓库卫生裁定（4 项未跟踪产物入不入历史） —— 仅用户本人
- **当前**：全部**仍未跟踪**：`docs/screenshots/`（19 文件 / 8.24 MB）、`bundle.js`、`idx.html`、
  `frontend/node_modules.stub/`（`git status --porcelain` 09-17 实测）。
- **09-17 接管新增第 5 项**：`be-r18/r18-evidence/`（未跟踪，6 个 txt：`full-after` / `full-final` / `INDEX` /
  `red-baseline-13` / `red-baseline-final` / `red-baseline`）。同属「不删、不代裁定」，依据见看板 §4V.2 第 5 条。
- **为什么不能我定**：入历史会永久增大仓库；加 `.gitignore` 改变全仓忽略语义。两者都是仓库级决定。
- **触发条件**：任一未来时点——① 有人需要 `docs/screenshots/` 作为答辩材料（则须入历史）；
  ② 新 Agent 因 `git add -A` 全线禁用而反复漏提交产物（则须改忽略规则）。
- **核对命令**：`git status --porcelain`（出现上述四项即仍未裁定）。
- **不做的代价**：`git add -A` 被明令禁止 ⇒ 每次提交都要显式列路径，**忘一次就丢一次**
  （今天差点丢掉 852 行的 `docs/system-architecture-2026-09-17.md`）。

## H5 `chroma_db/` 191 MB 反跟踪 —— 仅用户本人，且**必须等**
- **当前**：已跟踪且运行期写脏（09-17 实测 6 个二进制文件 `M`＋`chroma.sqlite3` `M`）。
- **硬前置**：**14 个工作树全部合完并回收之后**（09-17 接管时 10→14）。看板 §4G.5 已论证：现在反跟踪会
  连带删掉 `fe-trunk` / `fe-prims` 工作副本 ⇒ **三批在途期间绝对别碰**。
- **触发条件（可叫人）**：`git worktree list | Measure-Object -Line` 只剩 1 行，且 `git status` 里 `chroma_db/**` 仍为 `M`。
- **不做的代价**：每次 `git status` 长期脏 ⇒ 真实未提交改动被埋在里面，误提交 191 MB 二进制只是一次手滑。

## H6 一次都没 push 过（**这条最容易被"等忙完再说"拖死**） —— 仅用户本人
- **当前**：`codex/data-file-catalog` **无 upstream**（`git rev-parse --abbrev-ref '@{u}'` → fatal），
  而远端**两个都在**：`origin` = github、`gitee`。**全部历史只在这一块盘上。**
- **为什么不能我推**：需要凭据，且 push 是对外动作。
- **触发条件**：**没有"等阶段完成"这回事**——任何一次提交了成体系的工作（例如今天 `3f10b26` 的 1390 行文档）
  即为触发点。心跳若发现 `HEAD` 比上次记录新且仍无 upstream，应当叫人。
- **核对命令**：`git rev-parse --abbrev-ref --symbolic-full-name '@{u}'`（fatal ⇒ 未配 upstream）。
- **不做的代价**：磁盘或机器故障 ⇒ 代码、全部文档、两周协作记录**一起没了**。这一项的风险高于其它所有项。

## H7 三件套已过期，但 AGENTS.md 仍要求据其判断完成度 —— 需你定口径
- **当前**：`task_plan.md` / `progress.md` / `findings.md` 最后修改 **2026-09-16 10:01**
  （更正：我此前在别处写成 09-13，不准）。实际权威已是看板 + `docs/current-functionality-2026-09-10.md`。
- **触发条件**：任何新 Agent 入场——它读到的完成度是错的（09-17 实测：看板把已完成的 R13 记成"未开始"）。
- **二选一**：① 补到最新；② 在文件头显式标注"已停止维护，权威=看板 + current-functionality"。
- **不做的代价**：新 Agent 据过期计划重复派工或漏派工。这是我今天 §4U.4 四条就地订正的同源问题。

## H8 沙箱拦下的琐碎清理 —— 仅用户本人
- **当前**：`C:\Users\fengx\PycharmProjects\_commitmsg.txt`（今天提交信息用的临时文件，内容无敏感信息）。
  `Remove-Item` 被本机策略拒绝 ⇒ 只能你手动删。
- **09-17 接管新增第 2 项（本线自己造成的）**：主树根目录 `2026-09-15-orchestration-board.md`，
  **0 字节 / 未跟踪 / 9:47:09 生成**，根因是 PowerShell 里用 .NET `WriteAllBytes` 传相对路径——.NET 的 CWD
  **不跟随** `Set-Location`，于是照着 `docs/handoff/` 的文件名在仓库根建了空文件（与 H8 第一项同源，都是临时落盘路径没写绝对）。
  删它一条命令即可，但按红线本线不代删任何文件。

- **触发条件**：下次顺手清理时即可，不单独叫人。

## H9 演示日期 —— **已结案（2026-09-17 用户答复：至少一个月，不排 deadline）**
- 前端线 W5/W6/W7 已合（洞察→异常与告警、审批→报销自查、四张脸分开、Element Plus 移除，均 `[实测]`）。
- 若要对外演示，V 线还剩 **图谱撤一级入口**（`frontend/src/App.vue:48`、`:56` 仍在侧栏）与
  全屏登录 / 禁技术注解（判据见 `docs/frontend-plan-2026-09-14.md` §2）。
- **已答复并结案（2026-09-17）**：用户原话「这个不用管时间，至少还一个月」⇒ 不再追问演示时点，
  也**不得**拿演示日期给 H1/H3 造 deadline。V 线剩余项（撤图谱一级入口、全屏登录、禁技术注解）
  按 `docs/frontend-plan-2026-09-14.md` §2 的判据排序，不按日历排序。

## H10 心跳归属：两条总控线交接时的提醒责任 —— **交接项（Agent 可做，但必须让用户知情）**
- **性质**：本清单里唯一一条 Agent 能自己完成、却仍需用户点头确认的项。加它的原因是硬事实：
  heartbeat automation 的 `target_thread_id` 绑定单条线程，**换对话不会跟过去**。
- **当前（09-17 09:40 更新，新总控线）**：**新心跳已建** `automation-2`，`status=ACTIVE`、`FREQ=HOURLY`、
  `targetThreadId=01a0acfb-c674-7bc1-a875-0bde2366912b`。旧线心跳（`01a09dda-71ba-71d2-92e4-5b21d0d1f18e`）**仍 ACTIVE 未删**
  ⇒ **H10 未完**：处于「双心跳」期，这是先建新后删旧的预期代价，但**不能长期停在双心跳**（每小时报两遍 ⇒ 用户一烦整体关掉 ⇒ H6 永久失守）。
  只有用户能到旧线程说一句「接管完成」由旧线自行删除；新线不得越权删别人的心跳。
- **旧线口径存档**：交接前唯一在跑的心跳是「人工闸门提醒 · 企业智脑」，`FREQ=HOURLY;INTERVAL=1`，`status=ACTIVE`，
  新主控线程当时**没有心跳**（本节原有两行重复的「- **当前**」已合并，系旧线写盘手误）。
- **最短路径**：新主控读完五份文档、跑完自校命令后自建一条心跳指向自己；建成回旧线说一句
  「接管完成」，旧线随即删除自己的心跳。**顺序不能反**——先删后建会出现无人盯守的空窗。
- **核对命令**：`Get-ChildItem "$env:USERPROFILE\.codex\automations" -Recurse -File -Filter automation.toml`
  并读其中 `target_thread_id` 是否等于 `$env:CODEX_THREAD_ID`。同一条线程只能挂一条心跳，
  重复创建被拒是**预期行为**，不是失败。
- **不做的代价**：两种坏结果。**空窗**：用户以为有人在盯 H3/H6，实际旧线不再被叫、新线没建；
  **双心跳**：同一件事每小时报两遍，用户一烦整体关掉 ⇒ **H6 从此永久失守**，那是全清单风险最高的一项。

---

## 1. 优先级排序（按"不做的代价 ÷ 你要花的时间"）
| 序 | 项 | 你的耗时 | 不做的代价 |
|---|---|---|---|
| 1 | **H6 push** | 2 分钟 | 全部历史单副本，机器没了就没了 |
| 2 | **H3 Docker** | 10 秒 | 跨夜停摆（已发生过一次） |
| 3 | ~~H9 演示时点~~ **已结案**（用户 09-17：至少一个月） | — | 原「无法排期」不成立：改为按判据排期 |
| 4 | **H7 三件套口径** | 1 分钟 | 新 Agent 读错完成度，重复/漏派工 |
| 5 | **H11 本机验卡**（先跑，再决定要不要租卡） | 2 分钟 | 不验就分不清瓶颈在代码还是在 CPU 推理；跑通即省下 H1 |
| 6 | **H4 卫生裁定** | 3 分钟 | 显式列路径的纪律长期靠记性 |
| 7 | **H5 `chroma_db`** | 5 分钟 | 不急，**必须等在途合完** |
| 8 | H8 临时文件 | 顺手 | 无 |
| 9 | **H10 心跳归属** | 两边各一句话 | 提醒出现空窗，或双心跳被整体关掉 ⇒ H6 永久失守 |
| 10 | **H1→H2 租卡** | 20 分钟 | **仅当 H11 第一步探针失败才触发**；H11 跑通则本机够用，省下这笔 |

## 2. 提醒时点建议（心跳按此判断，未触发则保持安静）
- **每次都查**：H3（环境是否活着）、H6（是否仍无 upstream）。
- **推进到对应阶段才查**：**H11（R25+R26 落地后立刻查，它是 H1 的前置筛子）**、H1（仅当 H11 第一步探针失败）、H2（H1 之后）、H5（工作树剩 1 个时）。
- **只在有新人入场/新产生产物时查**：H4、H7。
- **不查**：H8（顺手项，不值得叫醒）。
- **交接责任**：H10 —— 新主控自建心跳并确认接管之前，H3/H6 仍由旧总控线心跳负责；接管后立刻移交，不得双挂。

---

## H11 让 ollama 容器真正拿到 GPU：**必须重启容器** —— **仅用户本人**（09-17 10:16 新增）
- **性质**：Agent 能改声明、能读事实，但**验证这最后一步要把 ollama 容器重启**，会打断正在跑的栈。
  当前 7 个容器已连续 Up 12–14 小时，且 `chroma_db/` 正被运行中的后端写入 ⇒ **Agent 不代做 `docker compose up/restart`**。
- **依据**：看板 §4V.8。宿主 4060 8 GB 在、WSL2 `/dev/dxg` 在、`nvidia` runtime 已注册，
  而 `docker-compose.yml:78` 的 ollama 服务**没有 `device_requests`** ⇒ 容器现在跑的是纯 CPU。
- **代价**：重启 ollama 会中断正在进行的推理；模型需重新加载。**不影响**数据库和已落盘的文件。
- **第一步（零风险探针，不动现有栈，建议先跑这个）**：起一个 `--rm` 一次性容器只验设备能否注入，
  不碰 ollama 服务、不占数据卷、跑完即删：

  ```powershell
  docker run --rm --gpus all --entrypoint sh ollama/ollama:latest -c "ls -l /dev/dxg; ls /usr/lib/wsl/lib | head"
  ```

  - **看到** `/dev/dxg` 与 `libcuda.so.1` ⇒ 声明路线成立，R26 补完 `device_requests` 后走第二步。
  - **报 `could not select device driver`** ⇒ Docker Desktop 的 GPU 支持没开或 toolkit 没接上，
    那就**直接回到 H1 租卡**，别再折腾本机，本机这条路的性价比归零。
- **第二步（R26 落地后，真机验卡，需要 1 分钟停服）**：

  ```powershell
  docker compose up -d ollama
  docker logs --tail 50 enterprise-brain-ollama-1 | Select-String 'inference compute'
  ```

  **判据**：日志出现 `inference compute id=cuda`（现在是 `id=cpu library=cpu`）＝通过。
  仍见 `id=cpu` ⇒ 失败，把该原始输出贴回总控线，R26 的「诚实降级」必须让它显式报错而不是静默 CPU。
- **注意口径**：`nvidia-smi` 在 docker-desktop VM 里 **not found 是预期行为**（WSL2 走 `/dev/dxg`+`libcuda` 通道），
  别用它判「没有 GPU」；容器内要看的是上面那两行。
- **不做的代价**：所有阶段 A/B 的延迟实测都建立在 CPU 推理上 ⇒ **数字系统性偏慢，
  据此得出的「瓶颈在代码不在算力」结论无法定档**，D 档收益永远是纸面值。

## H12 后端镜像落后主树 21 小时：任何容器端到端验收/真机跑分前必须重建 —— **仅用户本人**（09-17 18:20 新增）
- **性质**：这不是"要不要更新"的选择题，是**现在这台机器上跑的容器根本不是被测版本**。重建镜像跨小时、
  且会打断正在健康的栈 ⇒ Agent 不代做，须由你另开独立对话执行（本线程重启会丢任务状态）。
- **实证**（`[实测]` 2026-09-17 18:18:40，全程只读：未启动、未重启、未构建任何容器）：
  - `docker image inspect enterprise-brain:local --format '{{.Created}}'` → `2026-09-16T12:59:16Z`
    = 北京时 **09-16 20:59:16**，比当时早 **21 h 19 min**。
  - `docker ps` → backend / worker / scheduler 三个都 `Up 20 hours (healthy)`，用的正是该旧镜像。
  - 判别性标记：容器内 `/app/app/api/v1/chat.py` **2294 行**、`_authorized_source_rows` 出现 **0 次**；
    主树同文件 **2383 行**、该符号出现 **2 次**。该函数随 **R41 于今天 16:37（`571e0d6`）** 才进主树。
  ⇒ 容器**不含** R41（16:37）、R54（16:40）、R26b（16:54）三次合并。
- **代价**：不重建就做的任何容器端到端门禁、HITL 演示点击、R29 计时、105 题真机跑分，
  量的都是 09-16 晚上的产品，**结论对当前代码无效**；R36 判据③ 的基线会直接作废。
- **重建正解（环境陷阱，必须照做）**：`docker compose build migrate` —— **build `backend` 会静默空跑**，
  跑完看起来成功、镜像没变，是最坏的一种失败（假更新）。
- **与并发红线的关系**：不冲突。跑分窗口内禁止部署动作（R36 runbook §9）说的是"开窗后别动"；
  H12 说的是"开窗前必须已经到位"。顺序：先结 H12 → 再排 P-8 → 才允许开跑分窗口。
- **Agent 已代做的部分**：R36 runbook 已要求补前置 **P-8 被测镜像同源**（时间级 + 标记级双重机械判据），
  以后任何人跑分都会被这道闸门拦下，不必再靠记性。
## H13 未标注密级的上传按「1 级 = 最低公开」入库，是有意的吗 —— **需你定口径**（09-17 18:25 新增）
- **事实链**（全部只读实测）：`app/rag/indexing.py:114-124` 的 `_scope_int(value, default=1)` 在调用方给的
  `classification` 不可用时**兜底为 1**；`indexing.py:375-384` 的 `scope_metadata()` 把这个值随**每一条 chunk**
  写进元数据。⇒ 任何未显式标密级的上传，落库即带 `classification=1`，而不是"密级未知"。
- **为什么需要你**：这是产品口径而非代码缺陷。两种选择都自洽，后果相反——
  (A) 维持现状：未标注 = 公开级，任何部门内成员可见，检索不受阻；
  (B) 改为未标注 = 不可见（fail-closed 到密级维度），与 R17 空部门行、`filters.py:42` 的
      「no usable metadata is never visible」一致，但会让存量未标注文档**突然搜不到**。
- **与本班另一条发现的关系**：R57 要修的 `retrieval_pipeline.py:192` 补默认值，正是 (B) 路线在检索侧的镜像；
  我已实测它对**新建索引不可达**（因为入库总是带键），所以它属纵深防御；
  而本条 H13 决定的是**可达且每天都发生**的那一半。先定 H13，R57 的边界才不会被反过来推翻。
- **代价**：选 (B) 需配套一次存量文档密级回填（会改数据，属业主侧），否则旧文档集体消失于检索结果。
- **Agent 不会做的事**：不代你改 `indexing.py`，不代你跑回填。R57 派工里已明令执行层**禁止**触碰 `indexing.py`。
## H13 的**事实链订正**（09-17 18:46，总控亲读，取代上面那条 H13 的「事实链」段）

上面把成因写成 `app/rag/indexing.py:114-124` 的 `_scope_int(value, default=1)`。**这不是第一现场**，请以本节为准。

- **真正的第一现场 = API 契约层的默认值**：`app/api/v1/chat.py:1928-1931`
  ```
  @router.post("/upload")
  async def upload_document(file: UploadFile = File(...),
                            classification: int = Form(1),
                            department: str = Form(""),
  ```
  ⇒ 客户端**不填密级，文档即以 1 级（公开）入库**，且该端点对 `classification` **无任何校验**（无 `ge`/`le`、无白名单、不要求显式声明）。值随后在 `chat.py:2013-2018` 被**显式传参**交给 `retriever.add_document(...)`，一路写进每条 chunk 的元数据。`indexing.py` 的 `_scope_int` 只是把这个已经是 1 的值**再抄一遍**，不是它的责任。
- **同一条签名里存在方向相反的不对称（这条最值得你注意）**：同一个函数**故意忽略客户端传来的部门**，强制改写为上传者自己的部门，docstring 自己给了理由——"a department chosen by the client would let a caller publish into somebody else's results"；**而密级却允许客户端缺省成公开**。也就是说：
  **部门维度 fail-closed（服务端定），密级维度 fail-open（客户端可缺省）**，两套口径并排存在于同一个上传入口。
- **影响面**：这不是边角，是**每一次未点密级的上传都会发生**的默认行为；`app/rag/indexing.py` 的兜底与检索侧的 `, 1)` 都对**新建索引不可达**（入库总带键），唯二"可达且天天发生"的是 **本条 `Form(1)`** 与 **`app/common/rbac.py:45` 数据行 `fillna(1)`**（表格里密级列为空 ⇒ 按 1 级放行）。
- **请你就这两处定口径**，其余（检索侧/索引侧的兜底）无论你怎么选都只是纵深防御，Agent 会自己按纵深防御处理，不必劳你判断。
  - (甲) 维持「未标注 = 公开」：现状，零迁移成本，但意味着**任何忘记点密级的上传对公司全员可见**。
  - (乙) 改为「未标注 = 不可见 / 必须显式声明」：更安全，但**会让存量未标注文档从检索结果里消失**，须配套一次存量密级回填；且改 `Form(1)` 为必填会让不带该字段的客户端**直接 422**（含前端上传、测试内进程直调、`scripts/` 批量上传脚本），需要一次接口变更。
- **我仍不做的事**：不代你改 `chat.py:1929`、不改 `indexing.py`、不改 `rbac.py:45` 的密级兜底（R17 只改部门维度，密级维度等本条结论），不代跑回填。