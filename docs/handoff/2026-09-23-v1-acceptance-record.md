# V1 完工判定表（2026-09-23 · 总控线）

证据基线：主树分支 `codex/data-file-catalog` HEAD `a653151`（09-23 22:2x 复算，本页早稿写在 `c3ea42f`/3641 那一刻）。
可跑读数（全部由总控在**主树**亲跑，不采信执行层自述）：后端 **4101 passed / 39 skipped / 0 failed**（全量 233 s）· 前端 **971 passed / 48 files** · `lint:colors` **148 problems（0 errors，148 warnings）** · `npm run build` **EXIT=0**。

三态口径（沿用 R169 那一套）：**有证据** = 有可机读的断言在跑；**补不了** = 本机定义之外（真容器 / 真浏览器 / 真模型 / 真机计时）；**缺** = 今天确实没有。

## 1. V1 业务主线（逐条）

| 主线环节 | 状态 | 证据 / 缺口 |
|---|---|---|
| 登录 | 有证据（前端 9 格新补 + 后端 `POST /api/v1/login` `app/api/v1/auth.py:63`） | L1–L9 见 `frontend/src/components/__tests__/r169-login-screen.test.js`；L10 真账号真停用真 JWT = **补不了（真容器）** |
| 工作台 | 有证据 | 10 屏路由在 `frontend/src/router/index.js`；`ui/__tests__/states.test.js` 钉住空态不是打断 |
| 上传制度文档 | 有证据 | `POST /api/v1/chat/upload` `app/api/v1/chat.py:2896`；解析器 `app/rag/loader.py`（pdf / docx / txt / md）；上传与索引失败有稳定状态（R21 / R79 族用例） |
| 文档问答并看到来源 | 有证据（Q5） | `r150-chat-panel.test.js` + `lib/r150-event-claims.test.js` 钉住"一条命中：文件名可点、卡片带版本/密级/部门/相关度"，后端不发 sources 就不画出处卡 |
| 上传 Excel/CSV | 有证据 | `app/api/v1/data.py` 上传/列表/预览；`r169-data-chart.test.js` D1–D4 四张脸 |
| 指标统计与数据问答 | 有证据 | D3 统计取画像不取样行长度；“只有一行表头的 CSV 让数据面板渲染期抛错”已由 **R170** 修完并树（`dbfa1dc`，总控 09-23 亲跑） |
| 生成图表 | 有证据 | `app/tools/chart.py` 支持 bar / line / pie / radar；对话内出图只走带 Bearer 的 blob 通道（C1–C4）；C5 真像素 + 真落盘 = **补不了（真容器 + 真浏览器）** |
| 导出报告 | 有证据 | `app/tools/export.py` 出 PDF（含标题 / 日期 / 图表 / 生成时间 / 数据来源句）与 Excel；R2 / R3 格钉住"报告先经确认门、非图片产物另存仍是 blob:"；R4 真报告内容落真文件 = **补不了（真容器 + 真模型）** |
| 一条异常或告警结果 | 有证据 | 规则 CRUD 与手动巡检：`app/api/v1/alerts.py:342/371/382/396/407`；`test_phase4_alerts.py` / `test_alert_scan_scope.py`；“跨部门 manager 读到别部门告警正文”已由 **R176**（`3431053`）修完，R193 矩阵今日复跑为绿 |

## 2. V1 功能要求里"看不见摸不着"的那几条

| 要求 | 状态 | 说明 |
|---|---|---|
| 页面显示当前步骤 / 处理中 / 成功 / 失败 / 等待确认 | 有证据 | R168 把「等待确认」从对话流抽成一屏真待办（七张脸互不相同，零假行由赋值出口枚举钉死）`frontend/src/components/hitl/HitlPendingPanel.vue` |
| 用户可以取消或离开当前流，回来后不拿到另一轮的错误结果 | 有证据（今天补上） | `r169-chat-panel.test.js` Q7 取消两步四回执 + `lib/__tests__/r169-cancel-stream.test.js` Q5 读流层 7 枚；后端 `/ask/{id}/cancel` `chat.py:1522`、`/queue/{id}/cancel` `:3423` |
| 长任务不会无限等待 | **补不了** | 真机项，等 run6（阶段 A 判据①）；离线只有 `MODEL_CONCURRENCY_WAIT_SECONDS` 与档位超时用例 |
| 不出现明显死链接 / 空白页 / 技术异常串 | 有证据 | `lib/errcodes.test.js`(84) + `lib/no-bare-code.test.js`(17) + `insight-alerts.test.js` W7 四张脸两两不同；会话失效被踢回登录页零提示那一格已由 **R171** 并树（`9577b12`，判据被证伪后重写） |
| 无数据用空态、不许假数字填真实业务区域 | 有证据 | `ui/__tests__/states.test.js` + R168 的零假行机制；位图侧旧地球图已换（假统计烤死像素那事已结） |
| Multi-Agent 体现分类 / 文档 / 数据 / 图表 / 汇总 | 有证据 | `test_agent_collaboration.py` 族 + trace 载荷出口（R141 三处读数） |
| 演示数据必须明确是演示数据 | 有证据 | 评测与演示集命名 + 语料侧 `96 篇` 明写；105 题里 `must_contain` 查无出处的今天实测 **29 条**（不是早稿那句 55：R196 复测并用同一件函数精确复现了 55 的口径，差的 26 条由 R66 于 09-19 补语料后清掉，剩下的属 A/C 桶非语料可清）；改题要动评测集 = **业主单独批** |

## 3. ✅ 越权这一条今天结清了，接着压着 V1 的是真机窗口

早稿那句「在 R176 / R177 / R178 / R180 并树之前 V1 不具备宣布条件」已成立并结算：四枚修复件全部并树，R163 那两枚矩阵件也从 `eebaadb` 原样接回主干（**R193** `eef676c`）：**51 格矩阵今日真红 0 格**，13 格逐格归因到真实存在的主干 commit（归因由用例现场跑 git 证，不靠手写），另有三枚“摘掉守卫必须红”的运行时反证；矩阵之外的两格同形漏点（队列腿拒绝不落审计、`GET /documents` 那张“有但不能看”的假脸）由 **R194** `a653151` 收。⇒ 权限这一层今天可以对客户演示。**现在压着 V1 宣布的是另一类东西：真机门**——阶段 A 判据②（流式逐字）从没宣布验过、B / C（非越权两格）/ D（报告档可查回，开关仍关）/ E（环境矩阵 E1–E6）四道门目前全是 0，它们只能靠一次冷窗口的 **run6**（105 题串行，实测历史 0.71–1.30 h，不是早稿那句 2.5–4.7 h）取得。

## 4. 只有业主本人能做的（V1 收口的必要条件）

1. ~~起 Docker Desktop~~ → **本条今天被实证作废**：栈 09-23 开机后已连续跑 9 小时，backend / worker / scheduler / frontend / redis / postgres / ollama 全部 Up，六枚健康检查 healthy（R196 亲取）。真欠的是下面第 2 条。
2. **镜像线上态**：现役后端镜像 rev = `041108e`，落后主树 **64 枚**（另一枚建成未上栈的 `c053ddd` 还差 8 枚，容器摘要与两像都不相等 ⇒ 新像确实没替换）。——重建与 recreate **已授权给总控执行**（业主 09-23 口头），不再是业主侧动作；迁移 0013 也由总控跑（实测线上库 `pending_approvals_status_check` 仍是五枚词、schema_migrations 只到 0012，0013 确未落地）。
3. **改评测集**（今天实测 29 条查无出处，不是 55）仍需业主单独批，评测集被 `tests/test_evaluation_report.py` 钉着。~~开窗前设 standby~~ → R196 实测 `standby-timeout-ac` 与 DC 都已是 0x0 且未回弹，无需再动。
4. 裁 **R61 甲/乙**（部门列普查读数已交，本机净影响 0 枚）、**H20**、**D14 之后的新增闸门**。
5. 主树 8 项未跟踪垃圾删除（清单已给，删前沙盒模拟）；心跳 `automation-2` 的 `targetThreadId` 改指本线程（它仍指向已死的 `01a0acfb`，状态 PAUSED ⇒ 不会自动叫醒本线）。

## 5. 一句话结论

**V1 的离线部分今天见顶，越权不再是瓶颈**：功能件与证据都在树上（4101 后端 + 971 前端枚用例），阶段 C 越权那一格已按矩阵口径结清。剩下的不是写代码，是一个冷窗口：冻结代码 → 重建镜像到最终 HEAD → 落迁移 0013 → 重开 pgvector 双写窗 → **run6** → 逐格判读 A①②③④ 与 B / C / D / E。这一串全部在总控授权范围内，不需要业主动手。
