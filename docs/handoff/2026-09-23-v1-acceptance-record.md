# V1 完工判定表（2026-09-23 · 总控线）

证据基线：主树分支 `codex/data-file-catalog` HEAD `7b0ae26`（09-24 09:0x 复算；本页 §1/§2 两表写于 `a653151`/4101 那一刻，当时 run6 还没开窗，故只有 §3 之后是本班读数）。
可跑读数（全部由总控在**主树**亲跑，不采信执行层自述）：后端 **4101 passed / 39 skipped / 0 failed**（09-24 收窗后复跑 229.75 s，与上班同值 ⇒ 这次改文档没碰任何判据）· 前端 **993 passed / 49 files**（R198 落树后 +22 枚 / +1 件）· `lint:colors` **148 problems（0 errors）** · `npm run build` **EXIT=0**。

三态口径（沿用 R169 那一套）：**有证据** = 有可机读的断言在跑；**补不了** = 本机定义之外（真容器 / 真浏览器 / 真模型 / 真机计时）；**缺** = 今天确实没有。

## 1. V1 业务主线（逐条）

| 主线环节 | 状态 | 证据 / 缺口 |
|---|---|---|
| 登录 | 有证据（前端 9 格新补 + 后端 `POST /api/v1/login` `app/api/v1/auth.py:63`） | L1–L9 见 `frontend/src/components/__tests__/r169-login-screen.test.js`；L10 真账号真停用真 JWT = **补不了（真容器）** |
| 工作台 | 有证据 | 10 屏路由在 `frontend/src/router/index.js`；`ui/__tests__/states.test.js` 钉住空态不是打断 |
| 上传制度文档 | 有证据 | `POST /api/v1/chat/upload` `app/api/v1/chat.py:2896`；解析器 `app/rag/loader.py`（pdf / docx / txt / md）；上传与索引失败有稳定状态（R21 / R79 族用例） |
| 文档问答并看到来源 | 有证据（Q5） | `r150-chat-panel.test.js` + `lib/r150-event-claims.test.js` 钉住"一条命中：文件名可点、卡片带版本/密级/部门/相关度"，后端不发 sources 就不画出处卡 |
| 上传 Excel/CSV | 有证据 | `app/api/v1/data.py` 上传/列表/预览；`r169-data-chart.test.js` D1–D4 四张脸 |
| 指标统计与数据问答 | 有证据 | D3 统计取画像不取样行长度；“只有一行表头的 CSV 让数据面板渲染期抛错”已由 **R170** 修完并树（`dbfa1dc`，总控 09-23 亲跑） |
| 生成图表 | 有证据 | `app/tools/chart.py` 支持 bar / line / pie / radar；对话内出图只走带 Bearer 的 blob 通道（C1–C4）；C5 真像素 + 真落盘 = **补不了（真容器 + 真浏览器）** |
| 导出报告 | 有证据 | `app/tools/export.py` 出 PDF（含标题 / 日期 / 图表 / 生成时间 / 数据来源句）与 Excel；R2 / R3 格钉住"报告先经确认门、非图片产物另存仍是 blob:"；R4 真报告内容落真文件 = **补不了（真容器 + 真模型）** |
| 一条异常或告警结果 | 有证据 | 规则 CRUD 与手动巡检：`app/api/v1/alerts.py:342/371/382/396/407`；`test_phase4_alerts.py` / `test_alert_scan_scope.py`；“跨部门 manager 读到别部门告警正文”已由 **R176**（`3431053`）修完，R193 矩阵今日复跑为绿 |

## 2. V1 功能要求里"看不见摸不着"的那几条

| 要求 | 状态 | 说明 |
|---|---|---|
| 页面显示当前步骤 / 处理中 / 成功 / 失败 / 等待确认 | 有证据 | R168 把「等待确认」从对话流抽成一屏真待办（七张脸互不相同，零假行由赋值出口枚举钉死）`frontend/src/components/hitl/HitlPendingPanel.vue` |
| 用户可以取消或离开当前流，回来后不拿到另一轮的错误结果 | 有证据（今天补上） | `r169-chat-panel.test.js` Q7 取消两步四回执 + `lib/__tests__/r169-cancel-stream.test.js` Q5 读流层 7 枚；后端 `/ask/{id}/cancel` `chat.py:1522`、`/queue/{id}/cancel` `:3423` |
| 长任务不会无限等待 | **补不了** | 真机项，等 run6（阶段 A 判据①）；离线只有 `MODEL_CONCURRENCY_WAIT_SECONDS` 与档位超时用例 |
| 不出现明显死链接 / 空白页 / 技术异常串 | 有证据 | `lib/errcodes.test.js`(84) + `lib/no-bare-code.test.js`(17) + `insight-alerts.test.js` W7 四张脸两两不同；会话失效被踢回登录页零提示那一格已由 **R171** 并树（`9577b12`，判据被证伪后重写） |
| 无数据用空态、不许假数字填真实业务区域 | 有证据 | `ui/__tests__/states.test.js` + R168 的零假行机制；位图侧旧地球图已换（假统计烤死像素那事已结） |
| Multi-Agent 体现分类 / 文档 / 数据 / 图表 / 汇总 | 有证据 | `test_agent_collaboration.py` 族 + trace 载荷出口（R141 三处读数） |
| 演示数据必须明确是演示数据 | 有证据 | 评测与演示集命名 + 语料侧 `96 篇` 明写；105 题里 `must_contain` 查无出处的今天实测 **29 条**（不是早稿那句 55：R196 复测并用同一件函数精确复现了 55 的口径，差的 26 条由 R66 于 09-19 补语料后清掉，剩下的属 A/C 桶非语料可清）；改题要动评测集 = **业主单独批** |

## 3. 🔴 run6 已收窗（09-24 08:56:35）：五道门第一次拿到真机读数，A 行两绿两红

**本轮有效**：`evaluated=105/105`、缺题 0、多题 0、哨兵 0、`COLLECT_EXIT=0 / SCORE_EXIT=0`，容器镜像 rev 与主树同为 `7b0ae26`（第一次做到"读数与线上态同一枚 revision"）。分数：**`correctness 0.4762 → 0.5143`｜`evidence 0.6857 → 0.7143`｜`unsupported_claim_rate` 七连 0**；审批腿 19 题全部拿到终答、批准失败 0。逐格凭据在看板 **§4BQ**、判读口径在跟进单 **§93**。

| 门 | 本窗读数 | 结论 |
|---|---|---|
| **A①** 端到端 ≤90 s（问答档 n=64） | `median 27.9 s / p95 68.9 s / max 80.9 s` | ✅ 连续第二窗过线（run5 61.0 s）。整表 p95 143.8 s、分析档 212.1 s、报告档 146.6 s ⇒ **只算问答档绿** |
| **A②** 流式逐字无缺 | **0/105 成立**；`(text_frames,max_stream_frames)` = `(1,1)×85`、`(2,1)×19`、`(0,0)×1`；104/105 题 `missing_chars=extra_chars=0` | ❌ **不是量具坏**——一个字都没丢，但整段一次性到达。病根：唯一逐片出口 `app/api/v1/chat.py:2041` 是死道 ⇒ **R203** |
| **A③** 容器可见卡 | H11 已拿到卡 | ✅ |
| **A④** 逐类不退化 | `口径冲突 0.4211 → 0.3158`（19 题掉 2 题），其余十族零退步、五族进步 | ❌ 第二次由本判据抓到真退化（首次 run4 `doc-19`）⇒ **不许拿"总分涨了"抵账**，需归因单 |
| **C** 检索与缓存 | 越权格 ✅（R193 矩阵 51 格真红 0 格 + R194 两格同形漏点收口）；剩「缓存命中显式标注」「评测集分数不退化」两格 | 🟡 部分，未翻绿 |
| **D** 后台化与观测 | 报告档可查回／`usage` 非零／`sources` 在流里——**三格从未宣布验过**；`REPORT_LANE_VIA_QUEUE` 现网默认关 | 🔴 0 格 |
| **B / E** | 见计划书 §6 的 09-24 门的归属裁定 | ⚪ **移出 V1 门槛**（B=部署档位标定，物理前提在第一版硬件上不成立；E=上线实施清单，其越权格已由 C 覆盖） |

**🔴 事故 #40（机器侧）与一处读数陷阱**：09-23 23:34 → 09-24 07:40 整机掉进待机 **8 h 6 min**，而 `STANDBYIDLE` 的 AC 值实测一直是 `0x0` ⇒ **runbook「开窗前 `powercfg /change standby-timeout-ac 0`」那条前置是假绿**（第二次发生）。本班已改挂常驻 `SetThreadExecutionState(ES_CONTINUOUS|ES_SYSTEM_REQUIRED|ES_DISPLAY_REQUIRED)`（返回 `0x80000000`）；时延读数以逐题帧账 `wall_ms` 为准，**`evaluation-report.json` 的 `latency_ms.average=351 121 ms` 大于逐题最大值 ⇒ 被冻结污染，不采信**（`scripts/collect_evaluation_answers.py:128-135` 的实测兜底把 8 小时整段吃了进去）。

## 4. 现在压着 V1 宣布的，只剩这些

1. **代码单清零**：计划书真欠 **R48**（已定案路线甲，实估 1.5–2.5 人日）＋ 本班新立 **R202 / R203 / R204 / R205** ＋ §92 的 **R199 / R200 / R201**。波次：R199 ∥ R202 ∥ R204 → R203 ∥ R201 → R200 → R48。
2. **两格红的复验**：A② 要 R203 并树后一扇独占窗；A④ 的 `口径冲突` 退化要先归因（是 R122 装箱饿死检索，还是评测题本身撞那 29 条无出处）。
3. **D 行三格**：开 `REPORT_LANE_VIA_QUEUE` 跑一扇窗即可读，与 1、2 可共用窗口。
4. **pgvector 后两阶段**：`R59` 切读（等 **H20**）、`R60` 停写退役、`R90b` 迁移 0010 首装必停（按 D9 排窗后）。
5. **镜像与库已经是最新**（旧账订正）：现役后端镜像 rev = **`7b0ae26`**、`scripts/check_image_provenance.py` → **PASS**、迁移 **0013 已落地**（`schema_migrations` tail=0013、`pending_approvals_status_check` 六枚词含 `failed`）、前端镜像同批重建。⇒ 前一版这份文件第 4 节写的「落后主树 64 枚」「0013 确未落地」两句**就地作废**。

## 5. 只有业主本人能做的（收敛到四件）

1. 心跳 `automation-2` 的 `targetThreadId` 改指本线程（仍指向已死的 `01a0acfb`，状态 PAUSED ⇒ 不会自动叫醒本线）。
2. **批准改评测集**：105 题里 **29 条** `must_contain` 查无出处（不是旧那句 55），属 A/C 桶非语料可清；`tests/test_evaluation_report.py` 钉着，总控不许自己改题。
3. 裁 **H20**（pgvector 距离下限口径，决定 R59 能不能切读）与 **H13**（密级维度）。
4. 你早前提过一句「rename 数据库表」始终没给表名列名 ⇒ 需要时给我名字，否则总控不动 schema。

（8 项未跟踪垃圾按业主 09-21 授权「删就是了但删之前先在沙盒模拟」执行，属总控活；Docker 栈已实证在跑，六枚健康检查 healthy。）

## 6. 一句话结论

**V1 不再欠"一次冷窗口"，欠的是四件可派工的代码 + 两格复验**：越权已结清、A① 连续两窗过线、分数三项全涨、`unsupported_claim_rate` 七连零；A② 判红且病根已定位到一行的死道（R203）、A④ 判红且是判据真的有牙。B/E 两行已按"验的不是产品功能"移出 V1 门槛——连带把「首屏 ≤1 s」这类第一版硬件上物理不可达的口径从 V1 判据里摘掉，**这是收口，不是放宽**：挪出去的部分仍然在册、仍然要花钱验，只是不再挡 V1 宣布。

> **09-24 12:1x 复算（第六班第四格，主树 `424199c`）**：本页 §1/§2 的 4101 与 993 两个数是 `7b0ae26` 时刻的读数，今日三枚代码单并树后已上移——后端全量门 **4182 passed / 39 skipped / 0 failed**（`scripts/run_gate.py`，xdist -n 6 --dist loadfile，91.1 s，EXIT=0），前端 **1042 passed / 52 files**（基点 1017/50，+25 枚 / +2 件）、`lint:colors` 恒 148（0 errors）、`build` EXIT=0。并树的三枚是 R205a（时延记账修在采集器源头）与 R208（错误字典措辞 + 别名覆盖面反证钉），另本班把 R199/R201 结案入账（见看板 §4BT 二）。🔴 判定表各格**未因此翻绿**：A② 仍 0/105（等 R203）、C 两格与 D 三格仍未验（等合并复验窗）、B/E 已由业主移出 V1。
