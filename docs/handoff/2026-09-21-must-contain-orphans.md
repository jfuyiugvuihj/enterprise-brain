# R129 · 评测集 `must_contain` 查无出处：29 枚逐枚清单 + 每枚三选一处置建议

- 工单：R129（只读单）。生成时间 2026-09-21，Asia/Shanghai。
- 读数环境：主树 `C:\Users\fengx\PycharmProjects\企业智脑`，分支 `codex/data-file-catalog`，HEAD `e4e8c48`（脏项 13 枚，全部为本单**未触碰**的 `chroma_db/` 与前端/临时件）。工作树 `be-leg2` 全程 0 脏项。
- 解释器唯一：`C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe`（3.11.7，`sys.executable` 实测即此路径）。
- 取数工具：`scripts/check_eval_evidence_coverage.py`（本班主口径）。零模型、零网络、零连库、零起服务、**未跑 pytest**、**未跑评测**、**未改 `tests/fixtures/**` 与 `tests/test_evaluation_report.py` 一个字节**。
- 语料性质（贯穿全纸的成本假设）：`documents/` 那 95 篇是**业主从网上抓的假数据**。判「有没有出处」时它是事实源；但「补一篇 txt」在这套语料里成本低，在**真客户**那里不成立 —— 所有 B 建议都按这条定价。
- 旧账「55 条」已作废，本纸不引用；本纸所有数字均可用 §6 的命令重跑。

---

## 1. 总数复算：29 行 / 29 词 —— 与总控口径**一致**

| 项 | 本班实测 |
|---|---|
| 题源 | `tests/fixtures/business_evaluation_100.jsonl` = **105 行**（文件名 100 是历史名）/ **121 个** `must_contain` 词条 |
| 语料 | `documents/*.txt` = **95 篇**（主口径：PDF/xlsx/csv 不算出处） |
| 归一化 | NFKC → 去所有空白 → casefold；题面词与语料两侧同函数（`check_eval_evidence_coverage.py:109` `normalize()`） |
| 计行规则 | 一行只要缺任一词条即计入（`row_counts_as_missing_if_any_term_missing`） |
| **查无出处的行** | **29** |
| **查无出处的词** | **29**（口径指纹：行数 == 词条数） |

与上一班总控「29 行 / 29 词」**完全一致**，题号集合逐字相同（`approval-03 approval-05 chat-02 chat-08 chat-09 chat-11 chat-12 data-07 data-08 data-12 doc-07 doc-14 doc-15 doc-17 insight-05 insight-06 insight-07 report-03 report-07 report-08 report-09 scope-03 scope-04 scope-06 tool-03 unsupported-01 unsupported-02 unsupported-03 unsupported-04`），**无差集需要具名**。旁证：`tests/test_r94_eval_evidence_coverage.py` 的 `MISSING_IDS_29` 与本次复算同集合。

备查口径（不进本纸任何账）：`--include-pdf` 得 27（chat-02 靠 `refactor_guide.pdf` 的「之后」、insight-07 靠学习路线图 PDF 的「不确定性」被"救回"）；`--include-csv` 仍 29。

### 1.1 一句话结论（给拍板用）

1. 29 枚**不等于** 29 枚分。其中 **9 枚三跑都在得分**，动它们救不到 correctness，只救「出处」这条口径。
2. 全簇真实失分上限 = **+5.9 ~ +7.3 枚**（按"若孤儿行表现与非孤儿行同分布"估：非孤儿 39/76、40/76、41/76 vs 孤儿 9/29、8/29、9/29，见 §3 依据）＝ correctness **+0.056 ~ +0.070**，不是 +29/105 = +0.276。
3. 分档可救回：**语义安全 4~6 枚**（T2 改锚词，替换词实测三跑字面命中）。把"靠模型换词"与"靠放宽锚词"都算上，算术上限 **13 枚** ＝ §3 四档区间上沿之和；而按同分布估的真实上限只有 **+5.9~+7.3 枚** ⇒ **超出 7 的那约 6 枚是拿判分变松换的**，不是质量变好。本纸 §3/§4 对每一枚都标了这笔代价。
4. 29 枚里**真属于「客户制度域该有一页而 95 篇没有」的只有 2 枚**（approval-05、report-07）。把 29 枚当"补语料清单"交给业主，会得到一个假命题。

---

## 2. 逐枚清单（29 行，字段固定）

- `判分依据` 列：`金标＝` 后是 fixture 的 `answer` 字段原文；尺 = `app/quality/eval.py:_is_correct`，即 **`must_contain` 全部词条按**原样连续子串**命中答案文本**（AND 语义、**不做任何归一化** —— 与覆盖度检查的 NFKC 尺**不是同一把**，见 §5.3）；括注该题其余词条是否有出处。
- `缺的是` 列：`词形` = 同一条事实语料有、字面词不同（含同义/繁简/中英/数字格式/栏目词/短语被标点拆开）；`真缺` = 该事实在 95 篇里根本不存在；`不适用` = 真值不来自任何可上传文档（产品行为 / 会话自指 / 用户当场注入的假设值 / 报表栏目词）。
- `r3/r4/r5` 列：三跑该题 correctness，1=得分。**存件说明**：仓内只有 `docs/testing/evaluation-report.json`（run5 汇总，无逐题栏），run3/run4 汇总在仓外 `%TEMP%\evalrun\report-run{3,4}.json`。逐枚值本班用仓内判分器对三跑答案重算：run3=48/105=0.4571、run4=48/105=0.4571、run5=50/105=0.4762，**与三份汇总一字节不差** ⇒ 该列可当实测用，但它的来源是**答案件重算**，不是仓内逐题存件（仓内逐题存件：无）。

| 题id | group | must_contain 缺词 | 判分依据（expected_answer / 尺） | 缺的是 | r3/r4/r5 |
|---|---|---|---|---|---|
| doc-07 | doc｜问答·文档问答 | `计发` | 金标＝「按自然日计发」；尺＝AND(`自然日`,`计发`)，`自然日` 有据 | 词形（语料作「按自然日计算」`差旅费报销细则_2026版.txt:21`） | 1/1/1 |
| doc-14 | doc｜问答·文档问答 | `离职结算` | 金标＝「在离职结算前提交，由原部门负责人审批」；单词条 | 词形（手册:39 有「离职交接…财务结算（工资、报销、借款清零）」，两词被标点拆开） | 0/0/0 |
| doc-15 | doc｜问答·文档问答 | `不需要打印` | 金标＝「不需要打印，以电子原件入账」；单词条 | 词形（V2.1:65 作「无需再打印」）**＋语料互斥**：手册:65「电子发票需打印后附在报销单后」 | 1/1/1 |
| doc-17 | doc｜问答·文档问答 | `公司抬头` | 金标＝「一律开具公司抬头」；单词条 | 词形（细则:32 作「发票抬头须为公司全称」）**＋语料互斥**：V2.1:51 允许「公司全称或员工姓名」 | 1/1/1 |
| chat-02 | chat｜问答·多轮对话 | `之后` | 金标＝「部门负责人之后财务复核」；尺＝AND(`之后`,`财务`)，`财务` 有据 | 词形（细则:31 作「分管领导审批**后**」；「之后」二字 95 篇 0 命中） | 1/0/1 |
| chat-08 | chat｜问答·多轮对话 | `不叠加` | 金标＝「两人合住标准不叠加，按一间房计算」；单词条 | 词形（细则:17 作「按单人标准报销」；模型三跑答「不可以叠加」）＋语义缺口：语料限定「同性」 | 0/0/0 |
| chat-09 | chat｜问答·多轮对话 | `不矛盾` | 金标＝「不矛盾，只是补充了审批前置条件」；`requires_evidence=false` | 不适用（真值取决于上一轮；三跑正文均为「我没有上下文信息」） | 0/0/0 |
| chat-11 | chat｜问答·多轮对话 | `800元` | 金标＝「800元超出500元标准，超出部分需审批」；尺＝AND(`800元`,`审批`) | 不适用（用户当场注入的假设值；事实另一半「500元/晚」有据 细则:13） | 1/1/1 |
| chat-12 | chat｜问答·多轮对话 | `无法确认` | 金标＝「没有历史版本证据，无法确认」；`requires_evidence=false` | 不适用（要历史版本语料；另：登记表:11 规定的拒答词是「无口径登记」，与金标不同形，三跑 0 次被模型采用） | 0/0/0 |
| data-07 | data｜分析·Excel计算 | `前五` | 金标＝「返回前五部门及金额」；单词条 | 不适用（报表栏目词；事实**可由 CSV 算**：销售部148800/供应链部109120/研发部79980/市场部66960/客服部37200，第5-6名差12400 无并列歧义 —— 审计 §3.5 实算，本班复核 CSV=144行×9列且**无城市列**） | 1/1/1 |
| data-08 | data｜分析·Excel计算 | `小计` | 金标＝「分项给出小计金额」；单词条 | 词形（语料与报表通篇用「合计」，5 篇有据；「小计」95 篇 0 命中） | 0/0/0 |
| data-12 | data｜分析·Excel计算 | `变化率` | 金标＝「给出两季对比与变化率」；单词条 | 词形（`2026年Q1/Q2经营分析报告.txt` 通篇「环比/同比」，不用「变化率」）＋题面「本季度」口径未钉 | 0/0/0 |
| insight-05 | insight｜分析·主动洞察 | `长期未处理` | 金标＝「列出长期未处理单据」；单词条 | 词形（CSV `审批状态` 列字面就是「待审批（超 30 天未处理）」，5 张/32900 元 —— 语义同一、字面不同形） | 1/1/1 |
| insight-06 | insight｜分析·主动洞察 | `超标率` | 金标＝「返回超标率排名」；单词条 | 真缺（「超标率」95 篇 0 命中，登记表 cp/t 全不涉及）＋数据缺列（CSV 无城市列，三档 500/400/300 ⇒ 三种阈值三组互斥答案，**当前不可判**） | 0/0/0 |
| insight-07 | insight｜分析·主动洞察 | `不确定性` | 金标＝「给出带不确定性的预测区间」；尺＝AND(`预测`,`不确定性`) | 不适用（写作规范类断言；最接近的「95%置信区间」`MYBI数据分析平台_技术白皮书.txt:45` 说的是异常检测阈值） | 0/0/0 |
| approval-03 | approval｜问答·审批判断 | `无需超额审批` | 金标＝「未超500元标准，无需超额审批」；单词条 | 词形（事实可由三档标准推出 细则:12-15；该字面串 0 命中，「超额」只出现在手册:65 讲餐饮限额）**＋题面缺陷**：未限定城市档，480 元在二线/三四线**就是**超标 | 0/0/0 |
| approval-05 | approval｜问答·审批判断 | `需补开发票` | 金标＝「不能入账，需补开发票」；单词条 | 真缺（「补开/重新开具/换开」95 篇 0 命中）**＋方向相反**：细则:32 对遗失发票给的是「提供支付记录、行程单等辅助证明…酌情处理」，登记表 t-04 只管「抬头开错→退回重开」 | 0/0/0 |
| scope-03 | scope｜问答·跨部门权限 | `超出可见范围` | 金标＝「拒绝，超出可见范围」；`requires_evidence=false` | 不适用（本产品权限判定，写在代码里不写在客户文档；run5 正文改口成「当前没有工资明细数据文件」= 走错到数据道） | 0/0/0 |
| scope-04 | scope｜问答·跨部门权限 | `需单独授权` | 金标＝「默认不可见，需单独授权」；`requires_evidence=false` | 不适用（同上；语料最近形「需单独审批」`IT安全管理制度V3.1.txt` 讲的是生产区访问，主语对象都不是本题） | 0/0/0 |
| scope-06 | scope｜问答·跨部门权限 | `不可以` | 金标＝「不可以，权限判定不因角色放宽」；`requires_evidence=false` | 词形（模型三跑正文都是「管理员账号**不能**绕过部门限制」，与金标同义；「不可以」三字 95 篇 0 命中） | 0/0/0 |
| unsupported-01 | unsupported｜问答·无证据问题 | `无法确认` | 金标＝「无法确认」；`requires_evidence=false` | 不适用（考拒答行为）＋**行为缺陷**：run5 答「公司**没有**火星基地」= 把「查无」说成「不存在」 | 0/0/0 |
| unsupported-02 | unsupported｜问答·无证据问题 | `无法确认` | 金标＝「无法确认，知识库没有相关证据」 | 词形（模型三跑答「没有找到…相关信息」＝语义正确的拒答，只差字面） | 0/0/0 |
| unsupported-03 | unsupported｜问答·无证据问题 | `无法确认` | 金标＝「无法确认，缺少股权资料」 | 词形（模型三跑答「无法回答…不包含任何股权结构、股东信息或持股比例数据」≈金标后半句） | 0/0/0 |
| unsupported-04 | unsupported｜问答·无证据问题 | `无法确认` | 金标＝「无法确认，没有依据做此预测」；尺＝AND(`无法确认`,`预测`)，`预测` 有据 | 词形（同 02，「没有找到…相关信息」） | 0/0/0 |
| tool-03 | tool｜报告·工具调用 | `重新生成` | 金标＝「重新生成有效期内的链接」；`requires_evidence=false` | 不适用（导出链接有效期/重签是产品行为；三跑正文为「本轮未产出任何结论，请重试或补充数据范围。」21 字 = 管线桩答） | 0/0/0 |
| report-03 | report｜报告·报告生成 | `一页纸` | 金标＝「输出一页纸结论与依据」；单词条 | 不适用（输出格式要求；「一页」95 篇 0 命中） | 1/1/1 |
| report-07 | report｜报告·报告生成 | `审批路径` | 金标＝「列示超标项与建议审批路径」；单词条 | 词形＋真缺混合：链条有据（细则:31）；作为**报告栏目名**的「审批路径」从没被定义过（白皮书:36「流转路径」讲的是流程引擎） | 0/0/0 |
| report-08 | report｜报告·报告生成 | `未索引` | 金标＝「不能，未索引文档不入报告并需标注」；`requires_evidence=false` | 不适用（索引状态机是产品词；全语料「索引」仅 `产品技术手册.txt` 一处且指 Elasticsearch） | 1/1/1 |
| report-09 | report｜报告·报告生成 | `继续生成` | 金标＝「后台继续生成，完成后可查回」；`requires_evidence=false` | 不适用（后台任务是否随连接中断 = 纯产品行为；「后台」95 篇仅 `MYO_安全白皮书_对外版.txt` 一处指管理后台） | 0/0/0 |

`requires_evidence` 分布（本班实测）：29 枚中 **17 true / 12 false** —— 与审计 §3.7 的 A1+B8+C5+D3=17 一致。

---

## 3. 分档（4 档，每档给"动一下能救回几枚分"的区间）

估法说明：区间不来自直觉，来自**把候选锚词按仓内那把尺（原样子串、不归一化）打到三跑答案文本上**的命中向量。例：`doc-14` 候选「财务结算」= run3/4/5 全命中（111）⇒ 换这个词该题三跑全翻；`chat-08` 候选「按单人标准」= 101、「叠加」= 111 但会把「可以叠加」也判对 ⇒ 不计入。全部命中向量本班实测，非估计。

### T1「今天就在得分」= 9 枚：doc-07 doc-15 doc-17 chat-02 chat-11 data-07 insight-05 report-03 report-08
- 救回 correctness：**0 枚**（区间 0–1，唯一的 1 是 chat-02 在 run4 掉的那枚抖动 —— 它的缺词「之后」三跑里两跑被模型自发写出）。
- 真实收益在**出处口径**：这 9 枚有 8 枚 `requires_evidence=true`，补/改语料能把它们的 evidence 账做实（口径收益 8 枚，分数收益 0）。
- ⚠️ 本班读数提醒（**别把它当稳赚**）：T1 里 `insight-05`、`data-07`、`report-08` 三跑得分正文都带「未找到/无法提供『…』的具体数据」段落（run5 `insight-05` 原文：「关于长期未处理报销单 从现有文档中**未找到**…」），`doc-17` 得分正文是「可以开公司抬头**或员工个人抬头**」与金标「一律公司抬头」相反 ⇒ **命中判分词 ≠ 答对**。若为"清出处"去改锚词，等于把这 4 枚的假阳性固化。
- 估计依据：三跑 correctness 逐枚值（§2 表末列）+ 得分正文逐条人读。

### T2「改一个锚词即回」= 6 枚：doc-14 data-08 scope-06 unsupported-02 unsupported-03 unsupported-04
- 救回：**4~6 枚**。上限 6 = 六个替换词都实测三跑 111（`财务结算`/`合计`/`不能`/`没有找到`/`无法回答`/`没有找到`）；下限 4 = 若业主不许把拒答锚词放宽到「没有找到」（`unsupported-02/04`，理由：纯检索失败的正文也说「没有找到」，见 report-07/report-09 得分正文），仍剩 4 枚是真同义。
- 每枚都是**语义等价替换**，不新增判分漏洞 —— 除 02/04 两枚要业主明确接受"检索失败也算命中"的风险。

### T3「得动语料或动题面口径」= 5 枚：chat-08 data-12 approval-03 approval-05 report-07
- 救回：**1~4 枚**。拆解：`approval-03` 与 `approval-05` 各 1 枚（替换锚词「未超标」「不予报销」实测三跑 111，但**先把金标口径改对**才谈得上）；`chat-08` 0~1 枚（补语料写「不叠加」后还得模型真去引用；模型自发说「不可以叠加」，判分尺不认）；`report-07` 0~1 枚（只有先补《报告编制规范》把「审批路径」定义成栏目名，才可能从 0 变 1；只改锚词 = 判到一篇「未找到」，净收益记 0）；`data-12` 0 枚今日（三跑没人算出过 +29.63% 那个数，数值锚词全 000）。
- 这一档是"动一下最贵、也最容易被误当成补语料收益"的一档：它要求**业主裁定制度方向**（见 §4 的 B/A 条目），不是我们单方能动。

### T4「出处概念不适用，动语料零分收益」= 9 枚：chat-09 chat-12 insight-06 insight-07 scope-03 scope-04 tool-03 report-09 unsupported-01
- 救回（语料侧）：**0 枚**。跑分侧的 0~2 枚只可能来自别的单：`chat-09`/`chat-12` 要采集器重放多轮历史（三跑正文都在说「我没有上下文信息」）；`insight-07` 是 HITL 桩答（「本轮在『生成图表』前等待你确认」，R123 甲案范围）；`insight-06` 是 dataAgent 空返回（run5 正文 13 字：「【dataAgent返回】」）且题目在现有 CSV 上**不可判**；`scope-03/04` 属跨部门权限能力缺失（run5 该 category correctness = **0.0**/6 枚）；`tool-03` 是工具道桩答；`unsupported-01` 是**该修的缺陷**（把「查无」答成「不存在」），不许用改题掩盖。
- 估计依据：三跑正文逐条人读 + 仓内 `docs/testing/evaluation-report.json` 的 category 分解。

**整簇合账**：T1 0–1 ＋ T2 4–6 ＋ T3 1–4 ＋ T4 0–2 = **5~13 枚**，与 §1.1 第 2 条按分布估的 +5.9~+7.3 枚相互印证（区间上沿靠"把锚词放宽"换来，风险见各档）。

---

## 4. 三选一处置建议（每枚恰好一个；A 8 / B 7 / C 14）

- `A 改题面或 must_contain` = 改 `tests/fixtures/business_evaluation_100.jsonl` 的字面内容，**须业主或评测集归属单批准**；写"动哪几个字"= 精确到词。
- `B 补语料` = 改/加 `documents/` 下的 txt。**假语料侧成本一行到一篇；真客户侧不成立**，故 B 只用于"制度域本来就该写清"的两枚。
- `C 判为已知失分/已知例外，跑分报告里显式扣口径` = 不动题、不动语料，把这笔账记在 `docs/testing/evaluation-report.json` 的 `evidence_coverage` 栏与 `docs/handoff/2026-09-19-eval-evidence-audit.md`（建议新增 §4「R129 29 枚逐枚口径」附表，由总控落笔），并在真机跑分报告的口径段引用本纸。

| 题id | 处置 | 具体动作（字面级） | 成本／前置 |
|---|---|---|---|
| doc-07 | B | 把 `差旅费报销细则_2026版.txt:21`「伙食补助按自然日**计算**」写成「按自然日**计发**」 | 一处词；假语料零业务风险；需重建索引 |
| doc-14 | A | `must_contain`: `离职结算` → `财务结算`（金标 answer 同步改「在离职**财务结算**前提交」） | 三跑 111；语义无损（要的就是离职前那笔结算） |
| doc-15 | B | **先裁冲突**（§25.2 同源）：V2.1:65「无需再打印」vs 手册:65「需打印后附在报销单后」。裁定后统一为「**不需要打印**纸质版，以电子原件入账」 | 一处词×2 篇；不裁就补=造第二对互斥语料 |
| doc-17 | B | 先裁 V2.1:51「公司全称**或员工姓名**」与金标「一律公司抬头」；裁定后把细则:32 写成「发票抬头须为**公司抬头（公司全称）**」 | 同上；本题三跑虽得分，正文与金标相反 ⇒ 别当已清 |
| chat-02 | B | `差旅费报销细则_2026版.txt:31`「…分管领导审批**后**」→「…审批**之后**」 | 一字；同时消掉 T1 唯一抖动 |
| chat-08 | B | 细则:17 增「住宿标准**不叠加**」四字（并把「同性」限定与题面统一） | 一篇一行；模型仍需真引用才得分（T3 区间 0–1） |
| chat-09 | C | 记「多轮题未带历史 ⇒ 5 枚 chat 类失分不计语料账」；账落 §4 表 C 行 | 救法在采集器（另单），本单不动 |
| chat-11 | C | 记「用户当场注入的假设值不得作为出处证据要求」；evidence 栏扣口径 | 无 |
| chat-12 | C | 同 chat-09（另记：登记表:11 规定拒答词为「无口径登记」，与金标「无法确认」不同形，三跑 0 次被采用 —— 这是**产品级**话术未统一，不是语料缺） | 无 |
| data-07 | C | 记「报表栏目词，不是语料事实」；口径注明事实可由 CSV 实算（审计 §3.5 五部门数）＋提醒得分正文含拒答段 | 无 |
| data-08 | A | `must_contain`: `小计` → `合计` | 三跑 111；`合计` 在 5 篇语料有据，属把锚词挪回语料用词 |
| data-12 | A | 题面钉季度口径（「本季度=2026Q2、上季度=2026Q1，按发生月份归季」），并把 `must_contain`: `变化率` → `+29.6%`（或 `60240`） | 数值锚词三跑 000 ⇒ 今日救不回分，但这是**唯一不制造假阳性**的改法；须业主批准 |
| insight-05 | C | 记「以 CSV 状态列字面『待审批（超 30 天未处理）』为准」这条口径必须先钉（审计 §3.5 已列 2/4/5 三种答案风险）；分数已得，不动题 | 无（钉口径本身在数据侧，不属本单） |
| insight-06 | C | 判为已知失分：CSV 无城市列 ⇒ 「超标率」不可判；补 txt 也造不出缺失的列。账记「不可判题」清单 | 真修 = 数据侧加城市列（越本单域） |
| insight-07 | C | 记 HITL 桩答（R123 甲案范围内 18 枚之一），与语料无关 | 无 |
| approval-03 | A | 题面补城市（「在北京出差住 480 元一晚」），`must_contain`: `无需超额审批` → `未超标`＋`一线城市`（两词三跑均 111） | 金标现值在城市维度**是错的**（480 在二线/三四线即超标）⇒ 这条 A 是修题不是放水 |
| approval-05 | B | 给《差旅费报销细则（2026 版）》第四节补一款「票据补救顺序：发票缺失/开错一律**先补开（换开）**，确无法补开才凭支付记录＋行程单走辅助证明」 | **须业主先裁方向**：现文 细则:32 允许辅助证明酌情，与金标「需补开发票」方向相反；不裁就补 = 自相矛盾的第二条 |
| report-07 | B | 补一篇《经营分析报告编制规范》txt（15–20 行），定义超标项呈现栏位与「**审批路径**」栏目名及其取值 | 一篇新文档；真客户也可能有报告规范，成本比 approval-05 可控；不补则改锚词只会判到「未找到」 |
| report-03 | C | 记「输出格式要求不是语料事实」；分数已得 | 无 |
| report-08 | C | 记「索引状态机是产品词」；分数已得但正文含拒答段，别当已清 | 无 |
| report-09 | C | 记「后台任务是否中断 = 产品行为」，写在代码不在文档 | 无 |
| scope-03 | C | 记为跨部门权限能力缺失（run5 该 category 0.0/6），不许用改锚词把「没有数据文件」判成「拒绝越权」 | 无 |
| scope-04 | C | 同上 | 无 |
| scope-06 | A | `must_contain`: `不可以` → `不能` | 三跑 111；模型答「管理员账号不能绕过部门限制」＝金标原意 |
| tool-03 | C | 记工具道桩答（21 字「本轮未产出任何结论」），失分归管线不归语料 | 无 |
| unsupported-01 | C | **不许改题**：run5 答「公司没有火星基地」是把「查无」说成「不存在」，属真行为缺陷；记入跑分报告「已知失分（行为缺陷）」 | 无 |
| unsupported-02 | A | `must_contain`: `无法确认` → `没有找到` | 三跑 111；⚠️ 风险：该词也出现在纯检索失败正文里 ⇒ 须业主接受"拒答与检索失败同词"的放宽 |
| unsupported-03 | A | `must_contain`: `无法确认` → `无法回答` | 三跑 111；语义等价（金标后半句「缺少股权资料」模型也答到了） |
| unsupported-04 | A | `must_contain`: `无法确认` → `没有找到` | 同 02 风险；该题另一词 `预测` 已有据 |

**为什么不是"都建议改题"**：A 只给「同一事实、判分词与语料用词不同」的 8 枚（其中 3 枚还带风险标注）；B 给「制度域本就该写清」的 7 枚（其中 3 枚必须先裁冲突）；剩下 14 枚是**产品行为 / 采集 / 数据缺列 / 真行为缺陷**，动题或动语料都只会把缺陷藏进分数里 —— 那正是本单要防的事。

---

## 5. 三笔诚实账

### 5.1 没查到的（不编）
1. **run3/run4/run5 的逐题 correctness 在仓内无存件**。仓内只有 `docs/testing/evaluation-report.json`（run5 汇总，`total=105 / correctness=0.4762 / evidence=0.6857`，无逐题栏）。§2 末列来自仓外 `%TEMP%\evalrun\answers-run{3,4,5}.jsonl` 用仓内判分器 `_is_correct` 重算，并对账：run3 48/105=0.4571 与 `%TEMP%\evalrun\report-run3.json` 一致、run4 48/105=0.4571 与 `report-run4.json` 一致、run5 50/105=0.4762 与仓内 run5 一致。若这三份 answers 件被清理，该列在仓内不可再复算。
2. 审计 §3.4/§3.5 引的 `_audit/probe.py`、`data_check2.py`、`over.py` 三件一次性脚本本班**未在盘上找到**（与审计 §2.4 第 4 条同样的蒸发）。本班不复用它们，§2 的语料命中/上下文全部由 §6 命令现跑。
3. `be-leg2`@`114376b` 里**没有** `scripts/check_eval_evidence_coverage.py`、**没有** `docs/handoff/2026-09-19-eval-evidence-audit.md`（那两样只在主树 HEAD `e4e8c48`）⇒ 复算只能在主树跑，本纸却按工单写在 `be-leg2`（见 5.2）。
4. 没查「登记表『无口径登记』为何从未被采用」的根因（要读检索/prompt，越本单范围），只登记了三跑 0 次命中这个事实。

### 5.2 写域外的字节
- 本单只新增 **1 枚文件**：`be-leg2/docs/handoff/2026-09-21-must-contain-orphans.md`（即本纸），主树与 `be-leg2` 其他路径**零改动**；`tests/fixtures/**` 与 `tests/test_evaluation_report.py` 未被打开写入（只读）。临时件全在 `%TEMP%\r129\`。
- 待总控裁定的一处口径歧义：工单写「唯一可写路径 = `be-leg2\docs\handoff\…`」，又写「若在主树只读干活 ⇒ 把纸写到上面那个路径，主树那份由总控提交」。二者只能落一处，本班按**字面那一条**写进 `be-leg2`。若总控要主树版，复制本纸到 `企业智脑\docs\handoff\2026-09-21-must-contain-orphans.md` 即可（同规：CRLF、无 BOM、末行换行），**别两边同时留件**。

### 5.3 总控口径本身有问题的地方（不当调和，具名说）
1. **两把尺不是一把**：覆盖度检查用 NFKC+去空白+casefold 的子串，判分器 `app/quality/eval.py:_is_correct` 用**原样**子串。⇒ 「有出处」不保证「答得对」（模型多写一个空格就 0 分，`800 元` vs `800元` 即此例），「查无出处」里也混着**只有判分尺才会失分**的成分。本纸 §2 的命中向量全按判分尺跑，就是这个原因。建议总控把这句话写进跑分口径段。
2. **29 这个数把三种性质混在一起**：9 枚在得分、8 枚是"锚词该换字面"、9 枚是产品/采集/数据缺陷、2 枚才是语料真缺页（approval-05、report-07）。拿 29 去推「补 29 篇/改 29 题」的工程量是假命题。
3. **`must_contain` 只有 AND 语义**：判分器不支持"同义词任一命中"，所以本纸 A 类建议**每枚只能给一个词**，这也是 `无法确认` 5 枚无法用一条锚词统一清的原因（若哪天想加 OR 语义，那是**代码改动**，本纸按工单要求**不建议**用改判分器的方式让检查变绿）。
4. **`--include-pdf=27` 不是"更好"的口径**，本班同意主口径排除 PDF；但提醒：`refactor_guide.pdf` / `AI-Agent 学习路线图.pdf` 两篇既然在 `documents/` 目录里，就属于"业主会真上传"的东西 —— 要不要留在这套参考语料里，是**语料卫生问题**，建议另开一枚小单，而不是留在 §1 的括号里。

---

## 6. 复算命令（原样可重跑）

```
# 解释器（唯一）
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe   # sys.executable 实测 = 本行

# 环境变量：未设置任何影响口径的变量（本单不跑评测，RETRIEVAL_TIER / MODEL_MAX_CONCURRENCY / EVAL_SIDECAR 均未触碰）

cd C:\Users\fengx\PycharmProjects\企业智脑

# ① 主口径复算（29 行 / 29 词）＋逐枚缺词条 JSON 落 %TEMP%
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe scripts\check_eval_evidence_coverage.py --json "%TEMP%\r129\orphans.json"

# ② 三跑逐题 correctness（仓内判分器 + 仓外 answers 件；对账 48/48/50）
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\perrow.py       # 读 answers-run{3,4,5}.jsonl → correct.json
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\stats.py        # 汇总对账 + requires_evidence 17/12

# ③ 语料侧证据（词形 vs 真缺）
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\probe_corpus.py  # 每枚缺词的最长命中子串 + 原文窗口
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\terms_probe.py   # 40 个候选词在 95 篇里的命中篇数
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\corpus1.py       # 细则:8-36 / 登记表 全文 逐行
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\corpus2.py       # 登记表:31-52 / V2.1:45-70 / 手册:36-44,60-70
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\misc.py         # CSV 表头与行数（无城市列，144 行）

# ④ 锚词命中向量（分档区间的唯一依据）
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\rawflip.py       # 原样子串（=判分尺）
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\anchors.py        # 第二组候选
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\context.py       # 得分正文窗口人读
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\ctx34.py         # run3/run4 侧同项
C:\Users\fengx\PycharmProjects\企业智脑\.venv\Scripts\python.exe %TEMP%\r129\full.py          # data-07 / insight-05 run5 全文

# 未跑：pytest / docker / HTTP / 8001 / Ollama / GPU / 评测采集器 / 任何写仓内脚本
```

（临时件 `%TEMP%\r129\*.py` 由总控自处置；本单不删，删它们不在写域内。）
