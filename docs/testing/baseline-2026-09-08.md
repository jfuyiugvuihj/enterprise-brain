# 基线验收记录

日期：2026-09-09

## 当前证据

- 私有模型路由专项测试：`7 passed`。
- BM25 并发与依赖回退专项测试：`2 passed`。
- 全量后端测试：`227 passed, 3 skipped`。
- 全量测试耗时约 109 秒。
- 前端生产构建：`npm run build` 通过。
- 浏览器验收：登录、知识库多文件上传、原文预览、搜索、管理员删除、CSV 上传、单轮文档问答和多轮审批追问通过。
- 真实 Ollama 浏览器验收：自然语言多轮追问、图表确认、PDF 确认、取消、越界问题和模糊问题通过。
- 阶段 8 评估集：30 条业务问题，覆盖 10 类场景，并支持分类正确率、证据覆盖率和 P95 报告。
- 阶段 9 运行时：SSE 心跳、客户端取消、请求硬超时、P95 统计和性能报告脚本已具备。
- 阶段 10/19 运维：工作区备份、SHA-256 清单校验、安全恢复、JWT 轮换和受保护健康详情已具备。

## 环境限制

- 当前测试环境未安装 `jieba` 和 `rank_bm25`，代码已提供基础回退路径。
- PostgreSQL、Ollama、APS cheduler 生产依赖仍需在部署环境单独验收；当前测试使用内存数据库和离线模型回退。
- 测试输出仍有第三方库弃用告警，不阻塞当前回归。

## 后置事项

- 阶段 11 企业应用集成与 MCP 生产级接入继续按可选后置处理。
- PostgreSQL 真 dump/restore、完整灾备演练和生产监控接入需要在目标部署环境执行。
- 测试输出中的第三方库弃用告警不阻塞当前回归。
